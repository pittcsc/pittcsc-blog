<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="{{ '/css/style.css' | url }}" />
        <!--Twitter Card-->
    <meta name="twitter:card" content="summary">
    <meta name="twitter:site" content="@{{seo.twitter}}">
    <meta name="twitter:creator" content="@{{seo.twitter}}">
    <meta name="twitter:url" content="{{seo.baseURL}}{{page.url}}">

    <!--Schema-->
    <link rel="author" href="{{seo.baseURL}}">
    <link rel="publisher" href="{{seo.baseURL}}">
        
    <!-- Facebook OpenGraph -->
    <meta property="og:url" content="{{seo.baseURL}}{{page.url}}">
    <meta property="og:type" content="website">
    <meta property="og:site_name" content="{{seo.title}}">
    <meta property="og:locale" content="en_AU">
    <meta property="article:author" content="{{seo.baseURL}}">
    
    {%- block seo -%}
    <!--Twitter Card-->
    <meta name="twitter:title" content="{{title}}">
    <meta name="twitter:description" content="{{ seo.description }}">
    
    <!--Schema-->
    <meta itemprop="name" content="{{title}}">
    <meta name="description" content="{{ seo.description }}">
    
    <!-- Facebook OpenGraph -->
    <meta property="og:title" content="{{title}}">
    <meta property="og:description" content="{{ seo.description }}">
    {%- endblock -%}
    <title>Web Dev - A Pitt CSC Branch</title>
  </head>
  <body>
    <div><a href="https://pittcsc.org/" class="branch-link">A Pitt CSC Branch 🌿</a></div>
    <header>
      <h1 class="page-title">Web Dev</h1>
    </header>
    <main>
      <article>
        <ul class="post-list">
          {% for post in collections.posts %} 
            <li>
              <a href={{post.url}} class="post-block">
                <div class="post-title">{{post.data.title}}</div>
                <div class="post-author">By {{post.data.author}}</div>
                <div class="post-date">{{ post.date.toDateString() }}</div>
              </a>
            </li>
          {%- endfor %}
        </ul>
      </article>
    </main>
    <footer>&copy; 2021 Pitt Computer Science Club</footer>
  </body>
</html>